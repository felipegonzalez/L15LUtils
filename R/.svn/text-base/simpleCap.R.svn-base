simpleCap <-
function(x) {
    #print(x)
    if(is.character(x) ){
        s <- strsplit(x, " ")[[1]]
        #print(s)
        t <- s[s!=""]
        #print(t)
        #}
        paste(toupper(substring(t, 1,1)), tolower(substring(t, 2)),
          sep="", collapse=" ")}
}

